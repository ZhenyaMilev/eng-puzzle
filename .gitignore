# Залежності ставляться з package-lock.json, у репозиторії їм не місце —
# раніше сюди потрапило 1680 файлів node_modules
node_modules/

# Локальні артефакти запусків
test-results/
playwright-report/
.playwright-cli/
.netlify/

# Секрети живуть у змінних оточення Netlify і не мають потрапити в публічний
# репозиторій навіть випадковим `git add .`
.env
.env.*
*.pem
*-service-account*.json
serviceAccountKey.json

# Випадкові скриншоти й чернетки, які накопичилися в корені
/en-jelly-gel.png
/live-with-params.png
/preview-with-params.png
/tasks
eng-puzzle/image*.png

# Системне й редакторське
.DS_Store
.claude/
.mcp.json

# Знімки чужих акаунтів: пошта, історія розмов, словник. Репозиторій публічний,
# тож бекапи лишаються лише на машині, з якої їх зняли.
ops/backups/
